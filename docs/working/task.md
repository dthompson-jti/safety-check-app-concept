# Task: Add View Only Toggle to Dev Tools

## Context
Add a toggle to the Dev Tools UI to control `isViewOnlyMode`.

## Requirements
- [ ] Locate `DevToolsModal` (or equivalent).
- [ ] Add a new section or item for "View Only Mode".
- [ ] Bind toggle to `appConfig.isViewOnlyMode`.

## Output
-   `src/features/LateEffects/DevSettingsModal.tsx` (or whatever the file is named)
